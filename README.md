# dynamic-multistep-form-builder
A fully dynamic multi-step form builder in React JS powered by JSON configuration. Supports custom components, validation, step navigation, refs, portals, state management, and clean UI rendering without external UI libraries.
